# CLI 客户端（占位）

未来可实现的命令行客户端：

```bash
# 上传文本
clip send "hello world"

# 上传文件
clip send --file ./screenshot.png

# 获取最新
clip get

# 获取指定 ID
clip get 42

# 列出历史
clip list --limit 10
```

当前请使用 Web 界面或 API Token + curl。
